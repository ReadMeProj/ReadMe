import React from "react";

function SearchFilters(params) {
  return (
    <div className="searchFilterBox">
      Search options and filters will go here
    </div>
  );
}

export default SearchFilters;
